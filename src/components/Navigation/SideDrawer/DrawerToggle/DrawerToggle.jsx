import React from 'react';
import Wrapper from './DrawerToggleStyled';

const drawerToggle = ({ clicked }) => (
  <Wrapper onClick={clicked}>
    <div />
    <div />
    <div />
  </Wrapper>
);

export default drawerToggle;
